{% extends 'base.html.twig' %}

{% block title %}Description
{% endblock %}
{% block javascripts %}
	<script type="text/javascript" src="../../js/form.js"></script>
{% endblock %}
{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Description</h1>
		<li>{{ user.getFirstname() }}</li>
		<li>{{ user.getLastname() }}</li>
		<li>{{ user.getCardNumber() }}</li>
        {% for book in user.getBooks() %}
			<li>{{ book.getTitle() }}</li>
        {% endfor %}
        
        {# {% if user.getBooks() %}
		    {% for book in user.getBooks() %}
				<li>{{ book.getTitle() }}</li>
            {% endfor %}
        {% else  %}
            <li>Pas de livre emprunté!</li>
		{% endif %}
             #}
		

	</div>
{% endblock %}
